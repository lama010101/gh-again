import { GameSettings } from '@/types/game';

// Default number of images per game
export const DEFAULT_IMAGES_PER_GAME = 5;

// Default game settings
export const DEFAULT_GAME_SETTINGS: GameSettings = {
  timerSeconds: 180,
  hintsPerGame: 10,
};
